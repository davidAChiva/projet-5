{% extends "BackOffice/layout.html.twig" %}

{% block title %}
    Supprimer un administrateur | {{ parent() }}
{% endblock %}

{% block body %}
    <section class="row">
        <h2>SUPPRIMER L'UTILISATEUR : {{ admin.username }}</h2>
        <p>Voulez-vous confirmer la suppression de : {{ admin.username }} ?</p>
        {# Formulaire pour supprimer la spécialité de pays #}
        <form action="{{ path('manage_account_admin_delete', {'id': admin.id}) }}" method="post">

            <a href="{{ path('manage_account_admin') }}" class="btn btn-primary">Annuler</a>
            <input type="submit" value="Supprimer" class="btn btn-danger" />
            {# Pour générer le champ CSRF #}
            {{ form_rest(form) }}
        </form>
    </section>
{% endblock %}