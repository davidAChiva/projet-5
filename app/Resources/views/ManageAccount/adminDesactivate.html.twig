{% extends "BackOffice/layout.html.twig" %}

{% block title %}
    Désactiver un admin | {{ parent() }}
{% endblock %}

{% block body %}
    <section class="row">
        <h2>Désactiver : {{ admin.username }}</h2>
        <p>Voulez-vous désactiver le compte de : {{ admin.username }} ?</p>
        {# Formulaire pour supprimer la spécialité de pays #}
        <form action="{{ path('manage_account_admin_desactivate', {'id': admin.id}) }}" method="post">

            <a href="{{ path('manage_account_admin') }}" class="btn btn-primary">Annuler</a>
            <input type="submit" value="Désactiver" class="btn btn-danger" />
            {# Pour générer le champ CSRF #}
            {{ form_rest(form) }}
        </form>
    </section>
{% endblock %}
